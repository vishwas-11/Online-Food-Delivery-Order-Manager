{
  "version": 2,
  "builds": [
    {
      "src": "vite.config.*",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "scripts": {
    "build": "npm run build"
  }
}

